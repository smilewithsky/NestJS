# URL gốc của API NestJS (đã bao gồm prefix /api do main.ts setGlobalPrefix('api')).
# Biến KHÔNG có tiền tố NEXT_PUBLIC_ → chỉ đọc được ở phía server, không lộ ra browser.
API_BASE_URL=http://localhost:3000/api
